import { BaseHeader } from "@/components/base-header";

export function MovieListHeader() {
  return (
    <BaseHeader
      mainText="Rate movies"
      subText="Rate more movies for a more precise list"
      buttonText="Refresh"
    />
  );
}
